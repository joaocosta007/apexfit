# Banco PostgreSQL gratuito: Supabase, Neon ou Postgres local.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/apexfit?schema=public"

# Gere um segredo forte antes do deploy: openssl rand -base64 32
NEXTAUTH_SECRET="troque-este-segredo-em-producao"
NEXTAUTH_URL="http://localhost:3000"